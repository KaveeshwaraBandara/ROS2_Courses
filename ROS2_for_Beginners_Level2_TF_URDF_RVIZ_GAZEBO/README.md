# ROS 2 for Beginners Level 2: TF, URDF, RViz, Gazebo

This workspace focuses on robot description (URDF/Xacro), TF, visualization in RViz, and simulation in Gazebo.

## Packages

- `my_robot_description`: URDF/Xacro files and RViz configuration for the robot model.
- `my_robot_bringup`: Launch files and a Gazebo world for simulation.

## Key Files

- `src/my_robot_description/urdf/`: Robot model sources (URDF/Xacro).
- `src/my_robot_description/rviz/mobile_robot_config.rviz`: RViz display configuration.
- `src/my_robot_bringup/worlds/my_world.sdf`: Gazebo world.
- `src/my_robot_bringup/launch/`: Gazebo and display launch files.

## Build

```bash
colcon build
source install/setup.bash
```

## Run (Examples)

```bash
ros2 launch my_robot_bringup my_robot_gazebo.launch.xml
```

```bash
ros2 launch my_robot_bringup arm_gazebo_launch.xml
```

```bash
ros2 launch my_robot_description display.launch.py
```

```bash
ros2 launch my_robot_description arm_display.launch.xml
```

## Notes

- `build/`, `install/`, and `log/` are generated by colcon.
- Update Xacro files under `src/my_robot_description/urdf/` to modify the robot model.
